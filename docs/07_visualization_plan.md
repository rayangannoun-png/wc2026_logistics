# 07 — Visualization plan

Generated by `src/common/visualize.py` into `outputs/figures/`.

| File | What it shows | Report use |
|---|---|---|
| `part1_network_map.png` | Geographic map: open ports (green ▲, LED direct dashed), open depots (orange ■, soft grey lines), 16 stadiums (blue ●). | Show the optimal network structure — two hubs (West LA, East NY) + Vancouver for LED. |
| `part1_cost_breakdown.png` | Stacked bar of Part I cost components (ocean, depot fixed, depot handling, trucking ×3). | Ocean dominates (~\$310k of \$458k). |
| `part1_feu_by_class.png` | LED vs SOFT FEU, weight-ON vs weight-OFF. | **The headline figure:** 71 vs 40 FEU — the LED weight effect. |
| `part2_stage_split.png` | Stage-1 anticipatory vs stage-2 reactive expected cost. | The two-stage trade-off; notes 6/16 forced anticipatory. |
| `part2_feasibility.png` | Per-match window vs min transit, with the production=1-day line. | Visual proof of why 6 matches must be pre-positioned. |

## Suggested additions (if time)

- A bracket diagram of the R32 with venues/dates.
- A choropleth of stadium demand (m³) to show size dispersion (144–193 m³).
- A site-usage heatmap (which production site serves which venue most often).
- A tornado chart for each sensitivity table.

All figures use matplotlib only (no internet/basemap); coordinates come from
`nodes.csv`. Keep them readable at slide size.
